# core.autocrlf=true on the Windows laptop rewrites checked-out files to CRLF.
# Anything copied to the robot and executed by its shebang then fails with
# "/usr/bin/env: 'bash\r': No such file or directory", so keep shell scripts LF
# regardless of the platform they are checked out on.
*.sh text eol=lf
